# 家族选择联动动物池实现教程

## 本节目标


本节课只实现“开始界面家族选择 -> 本局游戏动物池”的联动：

1. 每个家族 Toggle 手动填写一个家族 ID。
2. 玩家确认选择后，保存选中的家族 ID。
3. 游戏场景启动时，`RanchManager` 读取保存的家族 ID。
4. 开局随机动物和每日抽卡动物池按这些家族过滤。


## 本节要修改的文件

只改下面 4 个脚本：

```text
Assets/Game/Scripts/UI/游戏开始界面/HangingSignToggleVisual.cs
Assets/Game/Scripts/UI/游戏开始界面/NChooseKToggleSelector.cs
Assets/Game/Scripts/Ranch/RanchManager.cs
Assets/Game/Scripts/Ranch/RanchContentPoolService.cs
```


## 第一步：给家族标签暴露 family 字段

打开：

```text
Assets/Game/Scripts/UI/游戏开始界面/HangingSignToggleVisual.cs
```

找到类开头的字段：

```csharp
[SerializeField] private Toggle toggle;
[SerializeField] private RectTransform sign;
[SerializeField] private RectTransform rope;
```

在下面新增：

```csharp
[Header("Family")]
[SerializeField] private string family;
```

再找到：

```csharp
private Vector2 signBasePos;
private Vector2 ropeBaseSize;
private Coroutine animCo;
```

在下面新增：

```csharp
private bool hasWarnedEmptyFamily;

public string Family
{
    get
    {
        if (string.IsNullOrWhiteSpace(family) && !hasWarnedEmptyFamily)
        {
            hasWarnedEmptyFamily = true;
            Debug.LogWarning($"[HangingSignToggleVisual] {name} 的 family 为空，请在 Inspector 中填写动物家族 ID。", this);
        }

        return family;
    }
}
```

这一文件只需要这两处改动。


## 第二步：确认选择时保存家族 ID

打开：

```text
Assets/Game/Scripts/UI/游戏开始界面/NChooseKToggleSelector.cs
```

找到 `[Header("Save")]` 下的字段：

```csharp
[SerializeField] private string selectedIndicesPlayerPrefsKey = "SelectedOptionIndices";
```

在下面新增：

```csharp
[SerializeField] private string selectedFamiliesPlayerPrefsKey = "SelectedAnimalFamilies";
```

找到 `SubmitCurrentSelection()` 方法里的保存逻辑：

```csharp
string selectedIndices = GetSelectedIndicesText();
PlayerPrefs.SetString(selectedIndicesPlayerPrefsKey, selectedIndices);
PlayerPrefs.Save();
```

改成：

```csharp
string selectedIndices = GetSelectedIndicesText();
PlayerPrefs.SetString(selectedIndicesPlayerPrefsKey, selectedIndices);
SaveSelectedFamilies();
PlayerPrefs.Save();
```

然后找到 `GetSelectedIndices()` 方法。它大概长这样：

```csharp
private List<int> GetSelectedIndices()
{
    List<int> selectedIndices = new List<int>();

    for (int i = 0; i < optionToggles.Count; i++)
    {
        Toggle toggle = optionToggles[i];
        if (toggle != null && toggle.isOn)
            selectedIndices.Add(i);
    }

    return selectedIndices;
}
```

在这个方法后面新增：

```csharp
private void SaveSelectedFamilies()
{
    if (string.IsNullOrEmpty(selectedFamiliesPlayerPrefsKey))
        return;

    List<string> selectedFamilies = new List<string>();

    foreach (Toggle toggle in optionToggles)
    {
        if (toggle == null || !toggle.isOn)
            continue;

        HangingSignToggleVisual signVisual = toggle.GetComponent<HangingSignToggleVisual>();
        if (signVisual == null)
            signVisual = toggle.GetComponentInChildren<HangingSignToggleVisual>(true);

        if (signVisual == null || string.IsNullOrWhiteSpace(signVisual.Family))
            continue;

        selectedFamilies.Add(signVisual.Family.Trim());
    }

    PlayerPrefs.SetString(selectedFamiliesPlayerPrefsKey, string.Join(",", selectedFamilies));
}
```

保存结果示例：

```text
SelectedOptionIndices = 0,1,2
SelectedAnimalFamilies = Hoofed,Bird,Carnivora
```

`SelectedOptionIndices` 仍然负责原来的图标回显；`SelectedAnimalFamilies` 负责游戏动物池联动。

## 第三步：RanchManager 读取玩家选择

打开：

```text
Assets/Game/Scripts/Ranch/RanchManager.cs
```

找到类开头的常量：

```csharp
private const string AnimalDataRoot = "Assets/Game/Data/Animals";
private const string ItemDataRoot = "Assets/Game/Data/Items";
```

在下面新增：

```csharp
private const string SelectedAnimalFamiliesPlayerPrefsKey = "SelectedAnimalFamilies";
```

找到 `InitializeFromScene()` 方法中的这两行：

```csharp
RefreshContentPools();
ranchMap = RanchSceneBinder.ResolveMap(ranchMap);
```

改成：

```csharp
ApplySelectedAnimalFamilies();
RefreshContentPools();
ranchMap = RanchSceneBinder.ResolveMap(ranchMap);
```

再找到公开的 `Initialize(...)` 方法中的这两行：

```csharp
RefreshContentPools();
ranchMap = map;
```

改成：

```csharp
ApplySelectedAnimalFamilies();
RefreshContentPools();
ranchMap = map;
```

然后找到 `RefreshContentPools()` 方法：

```csharp
private void RefreshContentPools()
{
    ContentPools.RefreshPools(
        autoPopulateOfferPoolByFamily,
        offerPoolFamilies,
        ref offerPool,
        ref abilitySpawnPool,
        ref itemRewardPool);
}
```

在它后面新增：

```csharp
private void ApplySelectedAnimalFamilies()
{
    if (!PlayerPrefs.HasKey(SelectedAnimalFamiliesPlayerPrefsKey))
    {
        return;
    }

    string savedFamilies = PlayerPrefs.GetString(SelectedAnimalFamiliesPlayerPrefsKey, string.Empty);
    List<string> selectedFamilies = ParseSelectedAnimalFamilies(savedFamilies);
    if (selectedFamilies.Count == 0)
    {
        return;
    }

    offerPoolFamilies = selectedFamilies;
}

private List<string> ParseSelectedAnimalFamilies(string savedFamilies)
{
    List<string> selectedFamilies = new List<string>();
    HashSet<string> selectedFamilySet = new HashSet<string>(StringComparer.OrdinalIgnoreCase);

    if (string.IsNullOrWhiteSpace(savedFamilies))
    {
        return selectedFamilies;
    }

    string[] familyParts = savedFamilies.Split(',');
    foreach (string familyPart in familyParts)
    {
        string family = familyPart.Trim();
        if (string.IsNullOrEmpty(family) || !selectedFamilySet.Add(family))
        {
            continue;
        }

        selectedFamilies.Add(family);
    }

    return selectedFamilies;
}
```

这一段的作用：

- 如果玩家保存过家族选择，就用 `SelectedAnimalFamilies` 覆盖本局 `offerPoolFamilies`。
- 如果玩家没有保存过，仍然使用场景中 `RanchManager` Inspector 里的默认家族池。

## 第四步：补充非 Editor 运行时过滤

打开：

```text
Assets/Game/Scripts/Ranch/RanchContentPoolService.cs
```

找到 `RefreshPools(...)` 方法中的这段：

```csharp
#if UNITY_EDITOR
if (autoPopulateOfferPoolByFamily)
{
    offerPool = RanchContentCatalog.LoadOfferAnimals(animalDataRoot, offerPoolFamilies);
}

abilitySpawnPool = RanchContentCatalog.LoadAnimals(animalDataRoot);
itemRewardPool = RanchContentCatalog.LoadItems(itemDataRoot);
#endif
```

改成：

```csharp
#if UNITY_EDITOR
if (autoPopulateOfferPoolByFamily)
{
    offerPool = RanchContentCatalog.LoadOfferAnimals(animalDataRoot, offerPoolFamilies);
}

abilitySpawnPool = RanchContentCatalog.LoadAnimals(animalDataRoot);
itemRewardPool = RanchContentCatalog.LoadItems(itemDataRoot);
#else
if (autoPopulateOfferPoolByFamily)
{
    offerPool = FilterAnimalsByFamily(abilitySpawnPool, offerPoolFamilies);
}
#endif
```

然后找到 `TryUseStartingAnimalsAsOfferPool(...)` 方法。它是类里最后一个方法：

```csharp
public bool TryUseStartingAnimalsAsOfferPool(IReadOnlyList<AnimalData> startingAnimals, ref List<AnimalData> offerPool)
{
    if (startingAnimals == null || startingAnimals.Count == 0 || offerPool.Count > 0)
    {
        return false;
    }

    offerPool = startingAnimals.Where(data => data != null).Distinct().ToList();
    return offerPool.Count > 0;
}
```

在它后面、类结束大括号前，新增：

```csharp
private List<AnimalData> FilterAnimalsByFamily(IReadOnlyList<AnimalData> animals, IReadOnlyList<string> families)
{
    var familyFilters = (families ?? Array.Empty<string>())
        .Where(family => !string.IsNullOrWhiteSpace(family))
        .Select(family => family.Trim())
        .Distinct(StringComparer.OrdinalIgnoreCase)
        .ToList();

    if (familyFilters.Count == 0 || animals == null)
    {
        return new List<AnimalData>();
    }

    return animals
        .Where(data => data != null && familyFilters.Any(data.HasFamily))
        .OrderBy(data => GetFamilySortIndex(familyFilters, data.Family))
        .ThenBy(data => data.Rarity)
        .ThenBy(data => data.DisplayName)
        .ThenBy(data => data.Id)
        .ToList();
}

private int GetFamilySortIndex(IReadOnlyList<string> familyFilters, string family)
{
    for (var i = 0; i < familyFilters.Count; i++)
    {
        if (string.Equals(familyFilters[i], family, StringComparison.OrdinalIgnoreCase))
        {
            return i;
        }
    }

    return int.MaxValue;
}
```

注意：这里使用了 `data.HasFamily`。如果学生项目里的 `AnimalData.cs` 还没有 `HasFamily(...)`，需要先完成“双家族动物支持”那节内容，或者临时改成只判断主家族：

```csharp
.Where(data => data != null && familyFilters.Contains(data.Family))
```

## 第五步：Unity 编辑器中配置家族标签

打开游戏开始场景：

```text
Assets/Scenes/GameStartSence.unity
```

找到家族选择框里的每一个家族 Toggle。

每个家族 Toggle 上应该有 `HangingSignToggleVisual` 组件。脚本改完并编译成功后，Inspector 中会出现新的 `Family` 字段。

逐个手动填写：

```text
有蹄家族 -> Hoofed
鸟类家族 -> Bird
食肉家族 -> Carnivora
鼠兔家族 -> Mouse-Rabbit
爬行家族 -> Crawling
水生家族 -> Aquatic
昆虫家族 -> Insect
```

如果有恐龙标签，但本节暂时不做 DLC 锁定，可以先填写：

```text
恐龙家族 -> Dinosaur
```

然后找到挂着 `NChooseKToggleSelector` 的对象，检查：

- `Selected Indices Player Prefs Key` 是 `SelectedOptionIndices`
- `Selected Families Player Prefs Key` 是 `SelectedAnimalFamilies`
- `Option Toggles` 中包含所有家族 Toggle
- `Required Selection Count` 是本局允许选择的数量，例如 `3`

## 第六步：Unity 编辑器中检查 RanchManager

打开游戏场景：

```text
Assets/Scenes/游戏界面.unity
```

选中场景里的 `RanchManager`。

检查：

- `Auto Populate Offer Pool By Family` 勾选。
- `Offer Pool Families` 可以保留默认值。

默认值只在玩家没有确认过家族选择时使用。玩家在开始界面确认后，会用 `SelectedAnimalFamilies` 覆盖本局动物池家族列表。

## 第七步：验证 PlayerPrefs 是否保存成功

进入 Play 模式。

在开始界面选择 3 个家族，例如：

```text
Hoofed
Bird
Carnivora
```

点击确认按钮。

为了临时验证，可以在 `NChooseKToggleSelector.SubmitCurrentSelection()` 里的 `PlayerPrefs.Save();` 后面加：

```csharp
Debug.Log("SelectedAnimalFamilies = " + PlayerPrefs.GetString("SelectedAnimalFamilies", ""));
```

控制台应该看到类似：

```text
SelectedAnimalFamilies = Hoofed,Bird,Carnivora
```

验证完成后，把这行 `Debug.Log` 删掉。

## 第八步：验证游戏动物池是否联动

推荐先用最容易观察的方法：

1. 临时把 `NChooseKToggleSelector` 的 `Required Selection Count` 改成 `1`。
2. 只选择 `Bird`。
3. 进入游戏场景。
4. 观察开局动物和每日抽卡候选动物。

预期结果：

- 开局随机动物来自 `Bird`，或第二家族包含 `Bird` 的动物。
- 每日抽卡刷新出的动物也来自 `Bird`，或第二家族包含 `Bird` 的动物。

也可以在 `RanchManager.ApplySelectedAnimalFamilies()` 里临时加日志：

```csharp
Debug.Log("ApplySelectedAnimalFamilies: " + string.Join(",", offerPoolFamilies));
```

进入游戏场景后，控制台应该显示玩家选择的家族。验证完成后删掉日志。

## 常见问题

### 确认后没有保存家族

检查：

- 家族 Toggle 上是否有 `HangingSignToggleVisual`。
- `HangingSignToggleVisual` 的 `Family` 字段是否手动填写。
- `NChooseKToggleSelector` 的 `Option Toggles` 是否包含这些 Toggle。
- 是否点击了确认按钮。

如果 `Family` 为空，控制台会出现：

```text
[HangingSignToggleVisual] xxx 的 family 为空，请在 Inspector 中填写动物家族 ID。
```

### 进入游戏后动物池没有变化

检查：

- `SelectedAnimalFamilies` 是否保存成功。
- `RanchManager.InitializeFromScene()` 是否在 `RefreshContentPools()` 前调用了 `ApplySelectedAnimalFamilies()`。
- 游戏场景中的 `RanchManager` 是否勾选了 `Auto Populate Offer Pool By Family`。
- 动物配置中的 `family` / `secondaryFamily` 是否和保存的家族 ID 一致。

### 非 Editor 打包后动物池为空

本节只补了一个从 `abilitySpawnPool` 过滤的运行时兜底。如果打包后为空，说明 `abilitySpawnPool` 在运行时没有预先带上动物数据。

后续可以改成：

- `Resources` 加载
- Addressables 加载
- 自定义配置表加载
- 在 Inspector 中序列化完整动物列表

本节课堂先完成编辑器内联动流程。

## 完成标准

完成后应满足：

1. 家族 Toggle 的 Inspector 中能看到 `Family` 字段。
2. 每个家族 Toggle 都手动填写了正确家族 ID。
3. 点击开始界面确认按钮后，会保存 `SelectedAnimalFamilies`。
4. 进入游戏场景时，`RanchManager` 会读取该值。
5. 开局随机动物来自玩家选择的家族。
6. 每日抽卡候选动物来自玩家选择的家族。
